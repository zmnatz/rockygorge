import { Grid, Typography } from "@mui/material";
import Link from "next/link";
import {Calendar} from "@/components/Calendar";
import Product from "@/components/Product";
import { NextLinkComposed } from "@/utils/nextLink";
import { Scores } from "@/components/Scores";

import links from "@/data/links.yml";
import forms from "@/data/forms.yml";
import StoreItems from "@/data/store.yml";

export const visibleStoreItems = StoreItems.filter(item => !item.hide);
export const visibleLinks = links.filter(item => item.header !== true && item.hide !== true);
export const visibleForms = forms.filter(item => item.hide !== true);

<Grid container spacing={2} sx={{ py: 2 }}>
  <Grid size={{xs: 12}} sx={{ textAlign: 'center' }}>
    ## 2025 Division III Atlantic Super Regional Champions
    ### 2023 Mid Atlantic Champions
    ### 2012 and 2014 Division II National Champions

    We are one of the premier rugby clubs in the Mid Atlantic. Competing in Division I and Division III men's rugby.

    [New Players: FIll out our contact form](./forms/contact) and a club leader will get in touch.

    [Join our alumni list](https://groups.google.com/forum/#!forum/rocky-gorge-alumni) 
    for social announcements, fan information, and old boy happy hours.
  </Grid>

<Grid container spacing={2}>
  <Grid size={{xs: 12, md: 6}}>
    <Grid container spacing={2}>
      {visibleStoreItems.map((item) => (
        <Grid size={{xs: 12, sm: 6, md: 6}} key={item.description}>
          <NextLinkComposed href={`/${item.name}`} style={{ textDecoration: 'none' }}>
            <Product title={item?.description}>
              {item.info}
            </Product>
          </NextLinkComposed>
        </Grid>
      ))}
    </Grid>

    <Grid container spacing={2} sx={{ py: 2 }}>
      {visibleLinks.map((item) => (
        <Grid size={{xs: 12, sm: 6, md: 6}} key={item?.id}>
          <NextLinkComposed href={item?.id} style={{ textDecoration: 'none' }}>
            <Product title={item?.title}>{item.description}</Product>
          </NextLinkComposed>
        </Grid>
      ))}
    </Grid>
    <Grid container spacing={2} sx={{ py: 2 }}>
      {visibleForms.map((item) => (
        <Grid size={{xs: 12, sm: 6, md: 6}} key={item?.id}>
          <NextLinkComposed href={`forms/${item?.id}`} style={{ textDecoration: 'none' }}>
            <Product title={item?.title}>{item.description}</Product>
          </NextLinkComposed>
        </Grid>
      ))}
    </Grid>
  </Grid>
  <Grid size={{xs: 12, md: 6}}>
    <Calendar />
  </Grid>
</Grid>
</Grid>